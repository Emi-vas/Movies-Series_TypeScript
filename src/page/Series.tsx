import React from 'react';
import Header from '../components/Header';

const Series = () => {
    return (
        <div>
            <Header page='series' />
            <h1>Series</h1>
        </div>
    );
};

export default Series;